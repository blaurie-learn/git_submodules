
/**
 * Add a submodule with:
 * 		git submodule add <git repository> extern/<repo name>
 *
 * 	adding in to the extern folder or something like it makes management of external deps a bit easier.
 *
 * Updating submodules from master commits:
 * 		git submodule foreach git pull origin master
 */

#include <collection/array.h>


int main(void) {
	return 0;
}
